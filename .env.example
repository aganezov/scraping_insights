# YouTube
YOUTUBE_API_KEY="AIzaSyC8GwCpBdgvgY7xiCWbgq3yezbpJBhZqkg"

# Reddit
REDDIT_CLIENT_ID=your_client_id
REDDIT_CLIENT_SECRET=your_client_secret
# Optional but recommended: unique and descriptive
REDDIT_USER_AGENT=insight-mine/0.1 (by u/your_username)

# External transcript provider (optional)
YTTI_API_TOKEN="692a49d3010e2f6af7b837ec"
YTTI_ENDPOINT=https://www.youtube-transcript.io/api/transcripts
# Optional: language hint (not required by provider today)
YTTI_LANG=en
# Optional: Webshare proxy creds for transcript fetching (no defaults)
YTTI_WS_USER=
YTTI_WS_PASS=

# ---- YouTube transcript provider (optional) ----
YTTI_API_TOKEN=your_basic_token_from_profile
YTTI_ENDPOINT=https://www.youtube-transcript.io/api/transcripts

# ---- Web scraping (explicit opt-in) ----
ALLOW_SCRAPING=0
# Optional: override UA for scraping (falls back to REDDIT_USER_AGENT or a safe default)
SCRAPE_USER_AGENT=insight-mine/0.2 (contact: you@example.com)

# ---- Selenium scraping (explicit opt-in; optional) ----
ALLOW_SELENIUM=0
SELENIUM_HEADLESS=1

# Language filtering (comma-separated; empty = no filter)
LANGS=en

# Web scraping (explicit opt-in)
ALLOW_SCRAPING=0
# Optional: override UA for scraping (falls back to REDDIT_USER_AGENT or a safe default)
SCRAPE_USER_AGENT=insight-mine/0.3 (contact: you@example.com)
